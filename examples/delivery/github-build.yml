name: Private application image build
on:
  workflow_dispatch:
permissions:
  contents: read
  id-token: write
jobs:
  build:
    # REPLACE both 40-zero pins with the same reviewed shared-template commit.
    uses: MikeeeGit/aks-delivery-templates/.github/workflows/build.yml@3610262472824c48576f29faa6a57f19f34b0808
    with:
      template-ref: '3610262472824c48576f29faa6a57f19f34b0808'
      client-id: ${{ vars.AZURE_ACR_BUILD_CLIENT_ID }}
      config: delivery.gateway.apps.json
      build-environment: image-build
