name: Private application image build
on:
  workflow_dispatch:
permissions:
  contents: read
  id-token: write
jobs:
  build:
    # REPLACE both 40-zero pins with the same reviewed shared-template commit.
    uses: MikeeeGit/aks-delivery-templates/.github/workflows/build.yml@a7e98749fe35b5dc5bc263705e8f510832ca2a08
    with:
      template-ref: 'a7e98749fe35b5dc5bc263705e8f510832ca2a08'
      client-id: ${{ vars.AZURE_ACR_BUILD_CLIENT_ID }}
      config: delivery.gateway.apps.json
      build-environment: image-build
