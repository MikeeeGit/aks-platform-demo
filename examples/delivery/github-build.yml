name: Private application image build
on:
  workflow_dispatch:
permissions:
  contents: read
  id-token: write
jobs:
  build:
    # REPLACE both 40-zero pins with the same reviewed shared-template commit.
    uses: MikeeeGit/aks-delivery-templates/.github/workflows/build.yml@13b9f32dcae343e183e64dcd655a97feda21929a
    with:
      template-ref: '13b9f32dcae343e183e64dcd655a97feda21929a'
      client-id: ${{ vars.AZURE_ACR_BUILD_CLIENT_ID }}
      config: delivery.gateway.apps.json
      build-environment: image-build
