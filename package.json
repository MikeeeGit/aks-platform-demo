{
  "name": "aks-platform-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Synthetic application for immutable image promotion between AKS slots.",
  "license": "Apache-2.0",
  "engines": {
    "node": ">=24 <25"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "start": "node dist/main.mjs",
    "test": "node --test --test-concurrency=1 tests/*.test.mjs",
    "verify": "npm run build && npm test"
  }
}
